@model DevLunch.Data.Models.Restaurant

@{
    ViewBag.Title = "Restaurants - " + @Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>

<div>
    <hr/>

    <dl class="dl-horizontal">

        <dt>
            Nothing to see here...yet
        </dt>

        <dd>

        </dd>
    </dl>
</div>
<p>
<p>
    <a href="@Url.Action("Edit", new {id = Model.Id})"><i class="fa fa-graduation-cap"></i></a>
</p>
<p>
    <a href="@Url.Action("Index", new {id = Model.Id})"><i class="fa fa-th-list fa-3x"></i></a>
</p>